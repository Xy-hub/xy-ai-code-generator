spring:
  profiles:
    active: local
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql5.sqlpub.com:3310/xytrest
    username: xtroot
    password: d6eQqUccZt1DGYvL
  application:
    name: xy-ai-code-generator-backend
server:
  port: 8001
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.xy.aicodegenerator.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
